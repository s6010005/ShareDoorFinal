@using shareDoor.Models.Extensions
@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()
        
    <ul class="nav navbar-nav navbar-right ">       
        <li>
            @Html.ActionLink("Καλωσήρθες " + User.Identity.GetUserNickName() + "!", "GetProfile", "Users", routeValues: null, htmlAttributes: new { title = "", @class = "mr-3" })

        </li>
        <li><a href="javascript:document.getElementById('logoutForm').submit()">Έξοδος</a></li>
    </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Εγγραφή", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink", @class = "text-white mr-4" })</li>
        @*<li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink", @class = "text-white" })</li>*@

        @*<li type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModalLong">

            </li>*@
        <li>
            <a class="text-white" href="@Url.Action("Login","Account")">Είσοδος</a>
        </li>
    </ul>

}








